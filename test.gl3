import "io"

fnc main() -> int32 {
    def bool x = true;
    def int32 y = -7200i32;
    def int8 yy = 4i8;
    def int16 w = 4i16;
    def int l = 999999999999i64;

    print("hi %b, %d, %fd, %y, %fy, %w, %fw, %l, %fl", x, y, y, yy, yy, w, w, l, l);
    return 0i32;
}
