fnc main() -> int32 {
    def int32* x = malloc(4*3);
    *(x) = 1i32;
    *(x+1) = 2i32;
    *(x+2) = 3i32;
    dbg_i32(*(x+2));
    def int32* secondElem = x+1;
    def int neg = 1;
    dbg_i32(*(secondElem-neg));
    return 0i32;
}
